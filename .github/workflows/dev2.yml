name: Updated Dev Deployment

on:
  push:
    branches: [ dev ]

jobs:

  create-envfile:
 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4.2.2

    - uses: SpicyPizza/create-envfile@v2.0.3
      name: Make envfile
      with:
        # envkey_DOCKER_COMPOSE_PREFIX: genji_pk-test
        # envkey_DOCKER_COMPOSE_FILENAME: docker-compose.dev.yaml
        envkey_DB_HOST: ${{ secrets.DB_HOST }}
        envkey_DB_PORT: ${{ secrets.DB_PORT }}
        envkey_DB_USER: ${{ secrets.DB_USER }}
        envkey_DB_PWD: ${{ secrets.DB_PWD }}
        envkey_DB_NAME: ${{ secrets.DB_NAME }}
        envkey_X_API_ROOT: ${{ secrets.X_API_ROOT }}
        envkey_X_API_KEY: ${{ secrets.X_API_KEY }}
        envkey_DISCORD_CLIENT_ID: ${{ secrets.DISCORD_CLIENT_ID }}
        envkey_DISCORD_CLIENT_SECRET: ${{ secrets.DISCORD_CLIENT_SECRET }}
        directory: ./
        file_name: .env.aaaaaaaa
        fail_on_empty: false
        sort_keys: false

    - uses: appleboy/ssh-action@v1.1.0
      with:
        host: ${{ secrets.GENJI_PK_HOST }}
        key: ${{ secrets.GENJI_PK_SSH_PRIVATE_KEY }}
        username: ${{ secrets.GENJI_PK_SSH_USER }}
        script: whoami